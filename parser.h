float parseAt(char *s, float x);
int intToStr(int x, char str[], int d);
void ftoa(float n, char* res, int afterpoint);
char *replaceAll(char *str, char *orig, char *rep);
void reverse(char* str, int len);
#include "parser.c"
